[project]
name = "logstat"
version = "0.1.0"
description = "logstat"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.46.1",
]

[tool.uv]
package = true

[project.scripts]
logstat = "app.main:main"
