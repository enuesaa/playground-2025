services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - 8080:8080
    volumes:
      - .:/app
    depends_on:
      - mysql
  
  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=test
    ports:
      - 3306:3306
    volumes:
      - mysqldata:/var/lib/mysql

volumes:
  mysqldata:
